# TEMPLATE ONLY — never commit real credentials.
# Fill with real values via:
#   kubectl create secret generic raw-data-layer-secret \
#     --namespace=raw-data-layer \
#     --from-literal=dolphindb-host=... \
#     --from-literal=dolphindb-password=... \
#     ...
# Or use Sealed Secrets / External Secrets / Vault for production.
# All keys are optional=true in deployment.yaml, so the pod starts even with
# placeholders (Binance/IB/DolphinDB disabled in config.multi.yaml by default).
apiVersion: v1
kind: Secret
metadata:
  name: raw-data-layer-secret
  namespace: raw-data-layer
  labels:
    app.kubernetes.io/name: raw-data-layer
type: Opaque
stringData:
  dolphindb-host: "dolphindb.default.svc.cluster.local"
  dolphindb-port: "8848"
  dolphindb-user: "admin"
  dolphindb-password: "CHANGE_ME"
  binance-endpoint: "wss://stream.binance.com:9443/ws"
  ib-host: "ibgateway.default.svc.cluster.local"
  ib-port: "7497"
